# expedia
1- client has a)lib:angulars libraries .
              b)app.js that has the controller.
              c)index.html has the html code that connected with the angular controller.

2- config has a)middleware.js that has the middelware function.
              b)routes.js that call function that get all data from the controller.

3- data has a)data.json that save all the json data.
           b)dataController.js that requier the data file & loop it and make respond for all data in json.
4- package.json that has all dependencies that will be required in code.
5- server.js created server.             
